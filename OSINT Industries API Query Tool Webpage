<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Query Tool</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .side-widget {
            display: inline-block;
            margin-right: 10px;
        }
        .widget img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
        }
    </style>
</head>
<body>
    <h2>Enter Email:</h2>
    <input type="text" id="inputEmail" placeholder="Enter email">
    
    <h2>Enter Phone Number:</h2>
    <input type="text" id="inputPhone" placeholder="Enter phone number">
    
    <button onclick="fetchData()">Submit</button>

    <!-- Loading GIF (replace the src with your actual GIF link) -->
    <img id="loadingGif" src="path_to_your_loading_gif.gif" style="display: none;" alt="Loading...">

    <h3>API Response:</h3>
    <table border="1" id="dataTable">
        <thead>
            <tr>
                <th>Website</th>
                <th>Profile Link</th>
                <th>Data</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>

    <script>
        function fetchData() {
            // Display the loading GIF
            document.getElementById('loadingGif').style.display = 'block';

            // ... [rest of the fetchData function]

            .then(data => {
                // Hide the loading GIF
                document.getElementById('loadingGif').style.display = 'none';
                renderData(data);
            })
            .catch(error => {
                // Hide the loading GIF
                document.getElementById('loadingGif').style.display = 'none';
                document.getElementById('dataContainer').textContent = 'Error fetching data: ' + error;
            });
        }

        // ... [rest of the script]
    </script>
</body>
</html>
